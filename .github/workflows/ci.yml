name: ci

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  cpp-format:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check formatting
        shell: pwsh
        run: |
          $vs = & "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe" `
                  -latest -property installationPath
          $cf = Join-Path $vs 'VC\Tools\Llvm\x64\bin\clang-format.exe'
          $files = Get-ChildItem -Recurse -Include *.cpp,*.hpp -Path engine
          & $cf --style=file --dry-run --Werror @($files.FullName)
          exit $LASTEXITCODE

  dotnet:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: nuget-${{ hashFiles('**/*.csproj', 'Directory.Build.props') }}
      - name: Check formatting
        run: dotnet format sotto.slnx --verify-no-changes
      - name: Build
        run: dotnet build sotto.slnx
      - name: Test
        run: dotnet test sotto.slnx --no-build

  build:
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        preset: [dev, release]
    steps:
      - uses: actions/checkout@v4

      - name: Build and test
        shell: pwsh
        run: |
          $vs = & "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe" `
                  -latest -property installationPath
          Import-Module "$vs\Common7\Tools\Microsoft.VisualStudio.DevShell.dll"
          Enter-VsDevShell -VsInstallPath $vs -SkipAutomaticLocation `
            -DevCmdArguments '-arch=x64 -host_arch=x64'
          cmake --workflow --preset ${{ matrix.preset }}
